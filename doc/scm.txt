##################################################
## SCM 
##################################################

##################################################
## GIT
##################################################
echo "# DART_DEMO_002" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main

## PATH 1 - NO EXTERNAL AUTH REQUIRED
git remote add origin https://github.com/aperezNWO/DART_DEMO_002.git
git push -u origin main

## PATH 2 - EXTERNAL AUTH REQUIRED
##  Create a new PAT with correct scope

Go to https://github.com/settings/tokens/new (make sure you're logged in as aperezNWO)
Fill in:

Note: codespaces-write
Expiration: 90 days
Scopes: ✅ repo (the top-level checkbox — this gives full repo access

git remote set-url origin https://aperezNWO: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@github.com/aperezNWO/DART_DEMO_002.git
git push --set-upstream origin main

##################################################
## DART / flutter 
##################################################

-- [_] INSTALL FLUTTER SDK.

    -- [_] https://docs.flutter.dev/install

##################################################
## CREATE CONSOLE PROJECT 
##################################################

(ORIGINALLY it WAS A FLUTTER PROJECT/ PENDING TO CREATE A CONSOLE PROJECT)
cd D:\usuarios\pabloperez\dev\dart\ping
dart create -t console dart_zero_endpoint

##################################################
## EXECUTE PROJECT
##################################################
 
## CONSOLE 

dart pub add shelf shelf_router mysql1 convert

dart pub add async

dart pub outdated 

dart pub get   

dart run bin/server.dart

##################################################
## FLUTTER  (NOT APPLY)
##################################################

flutter pub get   

flutter run bin/server.dart

flutter pub outdated 

Flutter pub upgrade --dry-run 

##################################################
## test output
##################################################

# Ping endpoint (other window)
curl -i http://localhost:8080/zero

# Mandelbrot (default bounds)
curl -i "http://localhost:8085/api/fractals/generate?kind=1"

# Julia with custom bounds
curl -i "http://localhost:8085/api/fractals/generate?kind=2&xMin=-1.5&xMax=1.5&yMin=-1.5&yMax=1.5&maxIterations=500"

# Barnsley Fern
curl -i "http://localhost:8085/api/fractals/generate?kind=3"

## DIJKSTRA
curl -i http://localhost:8085/GenerateRandomVertex_SpringBoot

## GET ALL PERSONS 
curl -i http://localhost:8085/api/data/getAllPersons

## GET ALL LOGS 
curl -i http://localhost:8085/api/data/getAllLogs


##################################################
## DOCKER
##################################################

# docker must be installed on desktop. it was from codespaces, only visual studio code extensions was installed
# dot included !!!
1) docker build -t dart-zero-endpoint .

# list images built / test 
2.1) docker image ls 
2.2) docker run -p 8080:8080 -e PORT=8080 dart-zero-endpoint
(cmd/java)
2.3) curl -i http://localhost:8080/zero
2.4) curl -i http://localhost:8080/health

# create a tag previous to publish
3) docker tag dart-zero-endpoint:latest apereznwogmail/dart-zero-endpoint:latest

# log into docker for publish
# get PAT from https://app.docker.com/accounts/apereznwogmail/settings/personal-access-tokens
## must return 'Login Succeeded'
# 4) echo "YOUR_PAT_HERE" | docker login --username apereznwogmail --password-stdin
docker login

# publish in docker hub
# https://hub.docker.com/
5) docker push apereznwogmail/dart-zero-endpoint:latest 

# URL FOR RENDER : https://dashboard.render.com/
6) docker.io/apereznwogmail/dart-zero-endpoint:latest


##################################################
##################################################
